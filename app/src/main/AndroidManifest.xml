<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- SMS/MMS Permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />

    <!-- RCS Permissions -->
    <uses-permission android:name="android.permission.READ_CELL_BROADCASTS" />

    <!-- Contact Permissions -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- Storage Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Phone/Call Log Permissions -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Internet & Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Background Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Schedule Background Tasks -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Microphone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".FanMessengerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_schemes"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FanMessenger"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FanMessenger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Chat Detail Activity -->
        <activity
            android:name=".ui.chat.ChatDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- New Message Activity -->
        <activity
            android:name=".ui.newmessage.NewMessageActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Contact Picker Activity -->
        <activity
            android:name=".ui.contacts.ContactPickerActivity"
            android:exported="false"
            android:parentActivityName=".ui.newmessage.NewMessageActivity" />

        <!-- SMS Broadcast Receiver -->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- MMS Broadcast Receiver -->
        <receiver
            android:name=".receiver.MmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_MMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.MMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Spam Detection Broadcast Receiver -->
        <receiver
            android:name=".receiver.SpamFilterReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.FanMessenGer.SPAM_CHECK" />
            </intent-filter>
        </receiver>

        <!-- Message Service -->
        <service
            android:name=".service.MessageService"
            android:exported="false" />

        <!-- Spam Filter Service -->
        <service
            android:name=".service.SpamFilterService"
            android:exported="false" />

        <!-- Firewall Service -->
        <service
            android:name=".service.FirewallService"
            android:exported="false" />

        <!-- Content Provider -->
        <provider
            android:name=".data.MessageProvider"
            android:authorities="com.FanMessenGer.messageprovider"
            android:exported="false" />

    </application>

</manifest>
